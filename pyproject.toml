[project]
name = "aurora-experiment"
version = "0.1.0"
description = "Refactored AURORA experiment – reproducible & CI-friendly."
readme = "README.md"
requires-python = ">=3.10"

dependencies = [
  "torch>=1.13.0",
  "datasets>=2.19.0",
  "pyyaml>=6.0",
  "transformers>=4.41.0",
]
